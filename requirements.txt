# =============================================================================
# ⚠️  DEPRECATED - Use specialized requirements files instead:
# =============================================================================
# 
# For building the knowledge graph (LOCAL):
#   pip install -r requirements_pipeline.txt
#
# For deploying the API (medical_kg_api_project/):
#   pip install -r requirements_api.txt
#
# =============================================================================

# Keeping this file for backwards compatibility - points to pipeline requirements
-r requirements_pipeline.txt